{
    "name": "cinepro-core",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "build": "tsc",
        "start": "npm run build && wrangler publish",
        "deploy": "npm run build && wrangler deploy",
        "preview": "npm run build && wrangler publish --dry-run",
        "format": "prettier --write src",
        "clean": "rimraf dist",
        "lint": "tsc --noEmit"
    },
    "dependencies": {
        "@omss/framework": "^1.1.19",
        "cheerio": "^1.2.0",
        "crypto-js": "^4.2.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20231218.0",
        "@types/crypto-js": "^4.2.2",
        "@types/node": "^22.7.4",
        "prettier": "^3.3.3",
        "typescript": "^5.6.2",
        "wrangler": "^3.45.0"
    }
}
